apiVersion: v1
kind: Pod
metadata:
  name: subpath-wo-overwrite-pod
  labels:
    name: subpath-wo-overwrite-pod
spec:
  volumes:
  - name: empty
    emptyDir: {}
  containers:
  - name: subpath-wo-overwrite-pod-pkad
    image: poznajkubernetes/pkad
    volumeMounts:
    - name: empty
      mountPath: /etc
      subPath: /etc
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 8080
